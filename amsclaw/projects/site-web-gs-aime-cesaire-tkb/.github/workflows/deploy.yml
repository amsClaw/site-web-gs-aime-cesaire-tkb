name: Deploy site vitrine

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Copy site files to root
        run: |
          rm -rf /tmp/deploy
          mkdir -p /tmp/deploy
          cp -r amsclaw/projects/site-web-gs-aime-cesaire-tkb/src/* /tmp/deploy/
          cp amsclaw/projects/site-web-gs-aime-cesaire-tkb/.nojekyll /tmp/deploy/
          cp amsclaw/projects/site-web-gs-aime-cesaire-tkb/README_SITE.md /tmp/deploy/README.md 2>/dev/null || true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: /tmp/deploy
      - id: deployment
        uses: actions/deploy-pages@v4
